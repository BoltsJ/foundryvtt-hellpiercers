<header class="sheet-header">
  <div class="charname">
    <input
      name="name"
      type="text"
      value="{{actor.name}}"
      placeholder="{{localize 'Name'}}"
      {{disabled (not editable)}}
    />
  </div>
  <div class="portrait">
    <img
      src={{actor.img}}
      class="portrait"
      data-edit="img"
      data-action="onEditImage"
      title={{actor.name}}
    />
  </div>
  <div class="hp">
    <label for="system.health.value">{{localize "HELLPIERCERS.ACTOR.FIELDS.health.label"}}</label>
    <div class="value">{{formInput
        fields.health.fields.value
        value=system.health.value
        max=system.health.max
        disabled=(not editable)
      }}</div>
    <div class="max">{{formInput
        fields.health.fields.max
        value=system.health.value
        disabled=(not editable)
      }}</div>
  </div>
  <div class="speed">
    <div class="label">{{localize "HELLPIERCERS.ACTOR.FIELDS.speed.label"}}</div>
    <div class="value">
      {{formInput fields.speed value=system.speed min=0 disabled=(not editable)}}
    </div>
  </div>
  <div class="scale">
    <div class="label">{{localize "HELLPIERCERS.ACTOR.FIELDS.scale.label"}}</div>
    <div class="value">
      {{formInput fields.scale value=system.scale min=0 disabled=(not editable)}}
    </div>
  </div>
  <div class="chartags">
    <div class="label">{{localize "HELLPIERCERS.ACTOR.FIELDS.tags.label"}}</div>
    <div class="value tags input-element-tags">
      {{#each system.tags}}
        <div class="tag">{{this}}</div>
      {{/each}}
      <a class="control edit-tags" data-action="onEditTags"><i class="fas fa-edit"></i></a>
    </div>
  </div>
</header>
